<aside
	class="col-3 border border-light bg-white p-2">
	{# ADMIN ONLY SECTION #}
	{% if is_granted('ROLE_ADMIN') %}
		<article class="card mb-3">
			<div class="card-header bg-primary text-white">
				⚙️ Administration
			</div>
			<div class="card-body">
				<p>
					<a href="{{ path('app_utilisateurs_index') }}" class="text-decoration-none">
						<i class="fas fa-users me-1"></i>
						Utilisateurs
					</a>
				</p>
				<p>
					<a href="{{ path('app_invitation_index') }}" class="text-decoration-none">
						<i class="fas fa-envelope me-1"></i>
						Envoyer invitation
					</a>

				</p>
			</div>
		</article>
	{% endif %}

	{# FORMATIONS (Admin + Gestionnaire) #}
	{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GESTIONNAIRE') %}
		<article class="card mb-3">
			<div class="card-header bg-success text-white">
				🎓 Gestion des Formations
			</div>
			<div class="card-body">
				<p>
					<a href="{{ path('app_formation_index') }}" class="text-decoration-none">
						<i class="fas fa-list me-1"></i>
						Liste des formations
					</a>

				</p>
				{% if is_granted('ROLE_ADMIN') %}
					<p>
						<a href="{{ path('app_formation_new') }}" class="text-decoration-none">
							<i class="fas fa-plus me-1"></i>
							Créer formation
						</a>

					</p>
					<p>
						<a href="{{ path('app_period_en_entreprise_index') }}" class="text-decoration-none">
							<i class="fas fa-building me-1"></i>
							Périodes en entreprise
						</a>
					</p>
				{% endif %}

				<p>
					{# <a href="{{ path('formation_periodes') }}" class="text-decoration-none">
																																																																																																															<i class="fas fa-building me-1"></i>
																																																																																																															Périodes en entreprise
																																																																																																														</a> #}
				</p>
			</div>
		</article>
	{% endif %}

	{# JOURS FÉRIÉS (Admin + Gestionnaire) #}
	{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GESTIONNAIRE') %}
		<article class="card mb-3">
			<div class="card-header bg-warning text-dark">
				🌴 Jours Fériés
			</div>
			<div class="card-body">
				<p>
					<a href="{{ path('app_jour_ferie_index') }}" class="text-decoration-none">
						<i class="fas fa-calendar me-1"></i>
						Calendrier
					</a>

				</p>

				<p>
					<a href="{{ path('app_interruption_index') }}" class="text-decoration-none">
						<i class="fas fa-pause me-1"></i>
						Interruptions
					</a>
				</p>
				{% if is_granted('ROLE_ADMIN') %}
					<p>

						{# <a href="{{ path('app_jour_ferie_sync_api') }}" class="text-decoration-none">
													<i class="fas fa-sync-alt me-1"></i>
													Synchroniser API
												</a> #}

					</p>
				{% endif %}
				{% if is_granted('ROLE_GESTIONNAIRE') %}
					<p>
						{# <a href="{{ path('jour_ferie_local_new') }}" class="text-decoration-none">
																																										<i class="fas fa-plus me-1"></i>
																																									Ajouter local
																																														</a> #}
					</p>
				{% endif %}
			</div>
		</article>
	{% endif %}
</aside>

{% extends 'base.html.twig' %}

{% block title %}PeriodEnEntreprise index
{% endblock %}

{% block body %}
	<h1>PeriodEnEntreprise index</h1>

	<table class="table">
		<thead>
			<tr>
				<th>Id</th>
				<th>Date_debut</th>
				<th>Date_fin</th>
				<th>Nombre Heures</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for period_en_entreprise in period_en_entreprises %}
				<tr>
					<td>{{ period_en_entreprise.id }}</td>
					<td>{{ period_en_entreprise.dateDebut ? period_en_entreprise.dateDebut|date('Y-m-d') : '' }}</td>
					<td>{{ period_en_entreprise.dateFin ? period_en_entreprise.dateFin|date('Y-m-d') : '' }}</td>
					<td>{{ period_en_entreprise.numbreHeures }}</td>
					<td>
						<a href="{{ path('app_period_en_entreprise_show', {'id': period_en_entreprise.id}) }}">show</a>
						<a href="{{ path('app_period_en_entreprise_edit', {'id': period_en_entreprise.id}) }}">edit</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="4">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

	<a href="{{ path('app_period_en_entreprise_new') }}">Create new</a>
{% endblock %}
